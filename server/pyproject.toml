[project]
name = "contravis-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bert-score>=0.3.13",
    "datasets>=5.0.0",
    "evaluate>=0.4.6",
    "fastapi>=0.138.1",
    "google-genai>=2.10.0",
    "llama-cloud>=2.1",
    "llama-parse>=0.5.20",
    "openai>=2.44.0",
    "pandas>=3.0.3",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.2",
    "python-dotenv>=1.2.2",
    "python-multipart>=0.0.32",
    "sentence-transformers>=5.6.0",
    "spacy>=3.8.14",
    "tiktoken>=0.13.0",
    "torch>=2.12.1",
    "tqdm>=4.68.3",
    "uvicorn[standard]>=0.49.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
    "ruff>=0.15.20",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

# main.py importa después de load_dotenv()/setup_logging() a propósito → no es E402.
[tool.ruff.lint.per-file-ignores]
"main.py" = ["E402"]

# FastAPI usa llamadas en defaults (Query/Depends/...) a propósito → no es B008.
[tool.ruff.lint.flake8-bugbear]
extend-immutable-calls = ["fastapi.Query", "fastapi.Depends", "fastapi.Path", "fastapi.Body", "fastapi.Header", "fastapi.Form"]
